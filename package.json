{
  "name": "showdesk",
  "version": "0.1.0",
  "private": true,
  "description": "ShowDesk local web application for live-production engineering observability.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "check": "node --check src/server.js && node --check src/atem-state.js && node --check public/transport.js && node --check public/signal-paths.js && node --check public/app.js && npm test",
    "bundle": "node scripts/package-runtime.js",
    "test": "node tests/signal-paths.test.js && node tests/atem-state.test.js"
  },
  "dependencies": {
    "atem-connection": "^3.5.0",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "postject": "^1.0.0-alpha.6"
  }
}
